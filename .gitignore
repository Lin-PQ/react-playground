# ----------------------------------------------------------------------------
# Logs (日志文件)
# ----------------------------------------------------------------------------
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ----------------------------------------------------------------------------
# Dependencies (依赖包)
# ----------------------------------------------------------------------------
node_modules
.pnpm-store

# ----------------------------------------------------------------------------
# Build Output (构建产物)
# ----------------------------------------------------------------------------
dist
dist-ssr
*.local

# ----------------------------------------------------------------------------
# Editor / IDE (编辑器配置)
# ----------------------------------------------------------------------------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# ----------------------------------------------------------------------------
# Environment Variables (环境变量 - 安全核心！)
# ----------------------------------------------------------------------------
# 务必忽略这些文件，防止 API Key 泄露
.env.local
.env.*.local
# 如果你的 .env 里包含密钥，把下面这行也取消注释
# .env

# ----------------------------------------------------------------------------
# Testing (测试相关)
# ----------------------------------------------------------------------------
coverage

# ----------------------------------------------------------------------------
# Tooling & Cache (工具缓存)
# ----------------------------------------------------------------------------
.eslintcache
*.tsbuildinfo

# ----------------------------------------------------------------------------
# Auto Generated Files (自动化插件生成的文件)
# ----------------------------------------------------------------------------
# 这些文件由 unplugin-auto-import 自动生成，每次运行可能会变，建议忽略
# 保持仓库 diff 干净
auto-imports.d.ts
src/auto-imports.d.ts
.eslintrc-auto-import.json

# 如果你以后用了 unplugin-vue-components (虽然你是React，但以防万一)
components.d.ts
src/components.d.ts

# ----------------------------------------------------------------------------
# Lock Files (锁定文件 - 强制只保留 pnpm-lock)
# ----------------------------------------------------------------------------
# 既然决定用 pnpm，就忽略其他包管理器的锁文件，防止混用
package-lock.json
yarn.lock
